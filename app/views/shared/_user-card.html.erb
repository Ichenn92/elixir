<li class="user-card">
  <div class="photo avatar-large">
    <%= cl_image_tag(@user.photo.key,width: 400, height: 300, crop: :fill) %>
  </div>
  <div class="name text-medium">@<%= user.nickname %></div>
  <button class="btn btn-primary">Add as friend</button>
</li>
